name: hello_dart_pkg
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/Richard-Van-Intania/hello_dart_pkg

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

